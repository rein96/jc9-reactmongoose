const init = {
    id: '',
    name: ''
}

export default (data = init, action) => {
    switch (action.type) {
        case 'LOGIN_SUCCESS':
            
            break;
    
        default:
            return data
    }
}